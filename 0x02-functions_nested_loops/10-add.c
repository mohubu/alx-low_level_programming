#include "main.h"
/**
 * add -Our function that adds two integers in our program
 * @i: first integer
 * @n: second integer
 *
 * Returns: the sum of the two numbers
 */
int add(int i, int n)
{
	int sum1;

	sum1 = i + n;
	return (sum1);
}
